

export default class Postproc {

  
  constructor({ votes, number, option, postproc}) {
    this.votes = votes;
    this.number = number;
    this.option = option;
    this.postproc = postproc;
  }
}
